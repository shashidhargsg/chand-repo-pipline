pipeline {
  agent any 
  environment {
        PATH = "C:/maven/apache-maven-3.8.5:$PATH"
        }

  stages {
     stage ('Build') {
       steps {
           sh 'mvn clean package'
       }
      }
  }
}
