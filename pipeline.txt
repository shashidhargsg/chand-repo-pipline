pipeline {
  agent any 

  stages {
    steps {
                git url:'https://github.com/citagi63/maven1.git', branch: 'master'  
            }
          }
     stage ('Build') {
       steps {
           sh 'mvn clean package'
       }
      }
  }
}