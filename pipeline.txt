pipeline {
  agent any 
  enviroment {
        PATH = "C:\maven\apache-maven-3.8.5"
        }

  stages {
     stage ('Build') {
       steps {
           sh 'mvn clean package'
       }
      }
  }
}
